# 初期導入フロー

1. Tailwind とパスエイリアスが有効であることを確認する。
2. `npx shadcn@latest init` を実行して初期化する。
3. テーマやベースカラーを選択し、生成ファイルを確認する。
4. 必要なコンポーネントを `npx shadcn@latest add <component>` で追加する。
5. 生成直後にビルドまたは型チェックを実行する。

## 注意点

- モノレポや独自エイリアス構成ではパス設定を見直す。
- 既存 UI ライブラリと競合する場合は責務を分ける。
